package interfaces.enums;

/*
* Enum that defines different main service types of agents
* Defined these for eliminating possible typos but still not using them most of the time
* */
public enum ServiceTypes {
    SUPERVISOR,
    REVIEWER,
    STUDENT,
    THESIS_COMMITTEE
}
